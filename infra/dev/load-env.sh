#!/bin/bash

export APP_ENV=dev
export DEV_MODE=true
export EXPOSE_BASE_URL=http://api.expose.alchemy.local

export AUTH_BASE_URL=http://auth.alchemy.local

export ASSET_CONSUMER_ACCESS_TOKEN=avoid
export UPLOADER_BASE_URL=http://api.uploader.alchemy.local

export EXPOSE_BASE_URL=http://api.expose.alchemy.local
export EXPOSE_STORAGE_ROOT_URL=https://s3.expose.alchemy.local/expose
