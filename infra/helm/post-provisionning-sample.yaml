auth:
  defaultUser:
    externalSecretName:
    # Secret must contain:
    # - DEFAULT_ADMIN_USER_USERNAME
    # - DEFAULT_ADMIN_USER_PASSWORD
  api:
    hostname: api-auth.phrasea.io
    baseUrl: https://api-auth.phrasea.io
  adminOAuthClient:
    externalSecretName:
    # Secret must contain:
    # - ADMIN_CLIENT_ID
    # - ADMIN_CLIENT_RANDOM_ID
    # - ADMIN_CLIENT_SECRET

expose:
  api:
    hostname: api-expose.phrasea.io
    baseUrl: https://api-auth.phrasea.io
    config:
      storageBaseUrl: https://minio.phrasea.io
      storageBucketName: expose
  adminOAuthClient:
    externalSecretName:
    # Secret must contain:
    # - ADMIN_CLIENT_ID
    # - ADMIN_CLIENT_RANDOM_ID
    # - ADMIN_CLIENT_SECRET
  client:
    hostname: expose.phrasea.io
    baseUrl: https://expose.phrasea.io
    oauthClient:
      externalSecretName:
      # Secret must contain:
      # - CLIENT_ID
      # - CLIENT_RANDOM_ID
      # - CLIENT_SECRET
      secret: ""
    mapbox:
      token: ""

notify:
  api:
    hostname: api-notify.phrasea.io
    baseUrl: https://api-notify.phrasea.io
  adminOAuthClient:
    externalSecretName:
    # Secret must contain:
    # - ADMIN_CLIENT_ID
    # - ADMIN_CLIENT_RANDOM_ID
    # - ADMIN_CLIENT_SECRET

uploader:
  persistence:
    existingClaim:
  api:
    hostname: api-uploader.phrasea.io
    baseUrl: https://api-uploader.phrasea.io
  adminOAuthClient:
    externalSecretName:
    # Secret must contain:
    # - ADMIN_CLIENT_ID
    # - ADMIN_CLIENT_RANDOM_ID
    # - ADMIN_CLIENT_SECRET
  client:
    hostname: uploader.phrasea.io
    oauthClient:
      externalSecretName:
      # Secret must contain:
      # - CLIENT_ID
      # - CLIENT_RANDOM_ID
      # - CLIENT_SECRET

report:
  api:
    hostname: report.phrasea.io
    baseUrl: https://report.phrasea.io

mailerDsn: ""

postgresql:
  externalSecretName:
  password: 'iHdB$$tyVGmT9Zj6shv'
  persistence:
    existingClaim:

rabbitmq:
  externalSecretName:
  password: '2rlTh4WyhyUZV7fEAnb$u4dqFchQDz'
  persistence:
    existingClaim:

minio:
  enabled: false

ingress:
  tls:
    wildcard:
      externalSecretName:
      # Secret must contain:
      # - tls.crt
      # - tls.key

globalConfig:
  externalConfigmapName:
