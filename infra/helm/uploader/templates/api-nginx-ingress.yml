apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: uploader-api-nginx-ingress
spec:
  rules:
  - host: {{ .Values.hostname.api }}
    http:
      paths:
      - backend:
          serviceName: uploader-api-nginx
          servicePort: 80
        path: /
