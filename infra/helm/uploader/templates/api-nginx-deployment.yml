apiVersion: apps/v1
kind: Deployment
metadata:
  name: phraseanet-service-uploader-api-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phraseanet-service
      tier: uploader-api-nginx
  template:
    metadata:
      labels:
        app: phraseanet-service
        tier: uploader-api-nginx
    spec:
      containers:
      - name: uploader-api-nginx
        image: {{ .Values.repository.baseurl }}/ps-uploader-api-nginx:{{ .Values.repository.tag }}
        terminationMessagePolicy: FallbackToLogsOnError
