apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: client-ingress
spec:
  rules:
  - host: {{ .Values.hostname.client }}
    http:
      paths:
      - backend:
          serviceName: uploader-client
          servicePort: 80
        path: /
