apiVersion: v1
kind: Secret
metadata:
  name: uploader-oauth-client-secret
stringData:
{{- with .Values.uploader.client.oauth_client }}
  CLIENT_ID: {{ .id }}_{{ .random_id }}
  CLIENT_SECRET: {{ quote .secret }}
{{- end }}
