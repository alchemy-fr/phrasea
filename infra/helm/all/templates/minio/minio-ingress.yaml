{{- if .Values.minio.enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: minio-ingress
spec:
  rules:
    - host: {{ .Values.minio.hostname }}
      http:
        paths:
          - backend:
              serviceName: minio
              servicePort: 80
            path: /
{{- end }}
