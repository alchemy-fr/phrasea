apiVersion: v1
kind: Secret
metadata:
  name: api-db-secret
stringData:
  DB_HOST: {{ quote .Values.postgresql.host }}
  DB_PORT: {{ quote .Values.postgresql.port }}
  DB_USER: {{ .Values.postgresql.user }}
  DB_PASSWORD: {{ quote .Values.postgresql.password }}
