apiVersion: v1
kind: Secret
metadata:
  name: api-rabbitmq-secret
stringData:
  RABBITMQ_HOST: {{ quote .Values.rabbitmq.host }}
  RABBITMQ_PORT: {{ quote .Values.rabbitmq.port }}
  RABBITMQ_USER: {{ quote .Values.rabbitmq.user }}
  RABBITMQ_PASSWORD: {{ quote .Values.rabbitmq.password }}
