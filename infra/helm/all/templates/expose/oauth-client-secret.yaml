apiVersion: v1
kind: Secret
metadata:
  name: expose-oauth-client-secret
stringData:
{{- with .Values.expose.client.oauth_client }}
  CLIENT_ID: "{{ .id }}_{{ .random_id }}"
  CLIENT_SECRET: {{ .secret | quote }}
{{- end }}
