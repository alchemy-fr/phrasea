repository:
  baseurl: alchemyfr
  tag: latest

volumes:
  configs:
    configmap_name: ps-configs

hostname:
  api:
  front:

aws:
  secret:
    name: aws-credentials
    key_name:
      access_key: access_key
      secret_key: secret_key

oauth:
  admin_client_id: admin-expose
  admin_client_random_id: expose123456
  admin_client_secret: expose123456
  client_id: expose
  client_secret: expose123456

params:
  app_id: expose
  db_user: user
  db_password: password
  rabbitmq_host: rabbitmq
  rabbitmq_port: 5672
  rabbitmq_user: rabbit
  rabbitmq_password: rabbit
  auth_base_url:
  storage_base_url:
  storage_bucket_name:
  report_base_url:
  dev_mode: false
  expose_base_url:
  matomo_host:

