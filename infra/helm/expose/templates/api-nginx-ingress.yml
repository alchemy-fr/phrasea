apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: expose-api-nginx-ingress
spec:
  rules:
  - host: {{ .Values.hostname.api }}
    http:
      paths:
      - backend:
          serviceName: expose-api-nginx
          servicePort: 80
        path: /
