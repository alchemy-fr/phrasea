repository:
  baseurl: alchemyfr
  tag: latest
volumes:
  configs:
    configmap_name: ps-configs

hostname:
  api:

oauth:
  admin_client_id: auth
  admin_client_random_id: auth123456
  admin_client_secret: admin123456

params:
  app_id: auth
  auth_base_url:
  registration_validate_email:
  db_user: user
  db_password: password
  mailer_url:
  rabbitmq_host: rabbitmq
  rabbitmq_port: 5672
  rabbitmq_user: rabbit
  rabbitmq_password: rabbit
  report_base_url:
