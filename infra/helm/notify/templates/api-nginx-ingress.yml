apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: notify-api-nginx
spec:
  rules:
  - host: {{ .Values.hostname.api }}
    http:
      paths:
      - backend:
          serviceName: notify-api-nginx
          servicePort: 80
        path: /
