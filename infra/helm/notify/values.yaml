repository:
  baseurl: alchemyfr
  tag: latest

volumes:
  configs:
    configmap_name: ps-configs

hostname:
  api:

oauth:
  admin_client_id: notify
  admin_client_random_id: notify123456
  admin_client_secret: notify123456

params:
    app_id: notify
    db_user: user
    db_password: password
    rabbitmq_host: rabbitmq
    rabbitmq_port: 5672
    rabbitmq_user: rabbit
    rabbitmq_password: rabbit
    auth_base_url:
    report_base_url:
