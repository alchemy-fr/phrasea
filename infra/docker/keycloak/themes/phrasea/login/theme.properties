parent=keycloak
import=common/keycloak

styles=css/login.css css/phrasea/login.css

meta=viewport==width=device-width,initial-scale=1

env.keycloakRedirectUrl=${env.KEYCLOAK_PASSWORD_CHANGE_REDIRECT_URL}
env.dashboardClientUrl=${env.DASHBOARD_CLIENT_URL}