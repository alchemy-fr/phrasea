name: Echo something fail

jobs:
  intro:
    steps:
      - run: |
          echo "intro"

  content:
    needs:
      - intro
    steps:
      - run: |
          echo "content"
          exit 1

  content-bis:
    needs:
      - intro
    steps:
      - run: |
          echo "content-bis"

  outro:
#    if: ${{ globals.foo == 'bar' }}
    needs:
      - content
      - content-bis
    steps:
      - executor: php
        run: |
          echo "outro".str_repeat('!', 2);

  never-called:
    if: ${{ globals.foo == 'another value' }}
    steps:
      - name: Dummy run
        executor: bash
        run: |
          exit 1
