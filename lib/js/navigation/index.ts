import {
    getPath,
    compileRoutes
} from './src/Router';
import {getCurrentPath, getFullPath} from "./src/utils";
import {Routes, RouteDefinition} from "./src/types";

export {
    getPath,
    compileRoutes,
    getCurrentPath,
    getFullPath,
}

export type {
    Routes,
    RouteDefinition,
}
