# Uploader

The Uploader service is part of the Alchemy ecosystem. Its primary role is to handle assets uploaded by authenticated users and to trigger other services so they can fetch the files as needed.

This service encompasses the following main components:
- Upload API (backend)
- Uploader client (frontend)
