
eight_points_guzzle:
    clients:
        download:
            options:
                timeout: 30
                verify: '%env(bool:VERIFY_SSL)%'
        consumer_notify:
            options:
                timeout: 20
                verify: '%env(bool:VERIFY_SSL)%'
