resources:
  App\Entity\FormSchema:
    attributes:
      normalization_context:
        groups:
          - formschema:index
      denormalization_context:
        groups:
          - formschema:write
    order:
      name: DESC
    shortName: form-schema
    collectionOperations:
      post:
        security: 'is_granted("EDIT_FORM_SCHEMA")'
      get:
        security: 'is_granted("EDIT_FORM_SCHEMA")'
    itemOperations:
      get:
        security: 'is_granted("EDIT_FORM_SCHEMA")'
      delete:
        security: 'is_granted("EDIT_FORM_SCHEMA")'
      put:
        security: 'is_granted("EDIT_FORM_SCHEMA")'
