import {FullPageLoader} from '@/components/ui/loader';

// Resolution is handled client-side by NotificationUriListener (AppShell).
export default function NotificationUriPage() {
    return <FullPageLoader />;
}
