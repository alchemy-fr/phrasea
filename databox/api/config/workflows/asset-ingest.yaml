name: asset-ingest

on:
  workflow_call:
    inputs:
      assetId:
        required: true
  asset_ingest:
    inputs:
      assetId:
        required: true

jobs:
  read_metadata:
    steps:
      - uses: App\Workflow\Action\ReadMetadataAction

#  integrations:
#    steps:
#      - uses: App\Workflow\Action\UploaderAckAssetAction
