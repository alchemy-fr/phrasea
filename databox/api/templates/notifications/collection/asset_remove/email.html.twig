{% block subject %}{{ 'collection.asset_remove.email.subject'|trans({'%collectionName%': collectionName}, 'notifications')|raw }}{% endblock %}

{% block body %}{{ 'collection.asset_remove.email.body'|trans({
    '%recipient%': (recipient.displayName|default(recipient.email))|e,
    '%author%': author|e,
    '%assetName%': assetName|e,
    '%collectionName%': collectionName|e,
}, 'notifications')|raw }}{% endblock %}
