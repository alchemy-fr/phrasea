{% block subject %}{{ 'asset.delete.email.subject'|trans({'%name%': name}, 'notifications')|raw }}{% endblock %}

{% block body %}{{ 'asset.delete.email.body'|trans({
    '%recipient%': (recipient.displayName|default(recipient.email))|e,
    '%author%': author|e,
    '%name%': name|e,
}, 'notifications')|raw }}{% endblock %}
