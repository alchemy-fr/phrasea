{% block subject %}New comment on {{ object }}{% endblock %}

{% block body %}
    <p>Hi {{ recipient.displayName|default(recipient.email) }},</p>
    <p><strong>{{ author }}</strong> commented on <a href="{{ url|default('#') }}">{{ object }}</a>.</p>
{% endblock %}
