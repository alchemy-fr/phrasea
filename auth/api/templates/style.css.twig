body {
    background-color: #efeeee;
}

.form-container {
    margin: 15px 0;
    background-color: #FFF;
    border-radius: 15px;
    padding: 15px;
    box-shadow: 0 0 15px rgba(0,0,0, 0.2);
}

{% if app_client_config.css -%}
{{ app_client_config.css|raw }}
{% endif %}
