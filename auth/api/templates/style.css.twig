body {
    background-color: #efeeee;
}

.form-container {
    margin: 15px 0;
    background-color: #FFF;
    border-radius: 15px;
    padding: 15px;
    box-shadow: 0 0 15px rgba(0,0,0, 0.2);
}

{% if app_client_config.css is defined -%}
{{ app_client_config.css|raw }}
{% endif %}

.idp-logo {
    margin: 0 auto;
}

.idp-logo-with-text {
    margin-bottom: 5px;
}

.idp-logo img {
    max-width: 50px;
    max-height: 50px;
}
