version: '3.4'

networks:
  internal:

services:
  api_php:
    image: api
    build:
      context: ./api
      target: upload_php
    networks:
      internal:
        aliases:
          - api
    environment:
      - APP_ENV
    volumes:
      - ./api:/srv/app

  api_nginx:
    image: nginx
    build:
      context: ./api
      target: upload_nginx
    networks:
      - internal
    volumes:
      - ./api:/srv/app
    ports:
      - 80:80
    depends_on:
      - api_php

  client:
    image: nginx
    build:
      context: ./client
      target: client_nginx
    networks:
      - internal

